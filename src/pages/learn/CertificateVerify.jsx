import { useState } from 'react';
import { Search, ShieldCheck, XCircle } from 'lucide-react';
import Seo from '../../components/ui/Seo.jsx';
import SectionCard from '../../components/portal/SectionCard.jsx';
import { verifyCertificate } from '../../lib/supabase/platform.js';
export default function CertificateVerify(){const[number,setNumber]=useState('');const[result,setResult]=useState(null);const[loading,setLoading]=useState(false);const[error,setError]=useState('');async function submit(e){e.preventDefault();if(!number.trim())return;setLoading(true);setError('');setResult(null);try{setResult(await verifyCertificate(number))}catch(e){setError(e.message)}finally{setLoading(false)}}return <><Seo title="Certificate Verification" description="Verify an InnoSpeak Global certificate." path="/verify-certificate"/><section className="bg-navy-gradient text-white"><div className="container-premium py-20"><p className="eyebrow text-gold-300">Credential verification</p><h1 className="mt-3 font-display text-4xl font-bold sm:text-5xl">Verify a certificate</h1><p className="mt-4 max-w-2xl text-white/70">Enter the certificate number to check whether the credential is recorded as issued.</p></div></section><div className="container-premium py-12"><SectionCard><form onSubmit={submit} className="flex flex-col gap-3 sm:flex-row"><input value={number} onChange={e=>setNumber(e.target.value)} placeholder="e.g. ISG-2026-..." className="flex-1 rounded-xl border border-navy-200 px-4 py-3 text-sm outline-none focus:border-gold-500"/><button className="btn-gold inline-flex items-center justify-center gap-2"><Search size={16}/>{loading?'Checking…':'Verify'}</button></form>{error&&<p className="mt-4 text-sm text-red-600">{error}</p>}{result?<div className="mt-6 rounded-2xl border border-emerald-200 bg-emerald-50 p-5"><p className="flex items-center gap-2 font-display text-lg font-bold text-emerald-900"><ShieldCheck size={20}/> Certificate verified</p><div className="mt-4 grid gap-3 text-sm text-emerald-900 sm:grid-cols-2"><p><strong>Certificate:</strong> {result.certificate_number}</p><p><strong>Student:</strong> {result.student_name}</p><p><strong>Course:</strong> {result.course_title}</p><p><strong>Issued:</strong> {new Date(result.issued_at).toLocaleDateString()}</p><p><strong>Status:</strong> {result.status}</p></div></div>:number&&!loading&&result===null&&<div className="mt-6 rounded-xl bg-navy-50 p-5"><p className="flex items-center gap-2 font-semibold text-navy-800"><XCircle size={18}/> No matching certificate was found.</p></div>}</SectionCard></div></>}
